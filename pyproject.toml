[project]
name = "miniclaudecode-repro"
version = "0.1.0"
description = "A distilled Claude Code agent loop"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.97.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
